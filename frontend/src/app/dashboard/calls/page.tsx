import ComingSoon from "@/components/ComingSoon";

export default function CallsPage() {
  return (
    <ComingSoon
      title="Calls"
      description="Inbound and outbound call logs, voicemail, and call routing."
      stage="Stage 3 (Voice Routing)"
    />
  );
}
