import ComingSoon from "@/components/ComingSoon";

export default function IntegrationsPage() {
  return (
    <ComingSoon
      title="Integrations"
      description="Connections to Twilio, billing, and other provider accounts."
      stage="a later stage (Provider Gateway)"
    />
  );
}
