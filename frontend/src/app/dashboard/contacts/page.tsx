import ComingSoon from "@/components/ComingSoon";

export default function ContactsPage() {
  return (
    <ComingSoon
      title="Contacts"
      description="Your saved contacts and call/message history with each one."
      stage="a later stage (not yet scheduled)"
    />
  );
}
