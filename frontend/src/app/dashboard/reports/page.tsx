import ComingSoon from "@/components/ComingSoon";

export default function ReportsPage() {
  return (
    <ComingSoon
      title="Reports"
      description="Usage, call quality, and vendor performance reports."
      stage="a later stage (not yet scheduled)"
    />
  );
}
