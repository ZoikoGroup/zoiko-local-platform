import ComingSoon from "@/components/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Account, team, and notification preferences."
      stage="a later stage (not yet scheduled)"
    />
  );
}
