import ComingSoon from "@/components/ComingSoon";

export default function VideoPage() {
  return (
    <ComingSoon
      title="Video"
      description="1:1 and small-group video calling with screen sharing."
      stage="a later stage (Phase 1 core feature per the roadmap)"
    />
  );
}
