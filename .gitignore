# Python
__pycache__/
*.pyc
venv/
.pytest_cache/

# Env / secrets
.env
*.env.local

# Node
node_modules/
.next/

# OS
.DS_Store
Thumbs.db

# Local dev tools (e.g. ngrok binary, downloaded manually - not project source)
.tools/

# Dev server logs (local nohup/background runs, not project artifacts)
*.log

# Ad-hoc plain-text extractions of the real .docx docs (scratch, for grepping -
# the .docx files in docs/ are the source of truth, these are disposable mirrors)
docs/*_full.txt
