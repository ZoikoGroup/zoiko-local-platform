fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
python-dotenv==1.0.1
pydantic-settings==2.5.2
passlib==1.7.4
bcrypt==4.0.1  # pinned: passlib 1.7.4 is incompatible with bcrypt>=4.1 (removed __about__ + wrap-bug self-test API)
python-jose[cryptography]==3.3.0
email-validator==2.2.0
google-auth==2.35.0
requests==2.34.2  # google-auth's HTTP transport needs this separately, not a hard dependency
pyotp==2.9.0
twilio==9.10.9
python-multipart==0.0.9
livekit-api==1.2.0
tzdata==2024.2
boto3==1.35.36  # S3-compatible object storage (recordings) - see app/integrations/storage/s3.py
stripe==15.4.0  # Identity verification (KYC/KYB) - see app/integrations/kyc/stripe_identity.py
pywebpush==2.3.0  # Web Push (VAPID) - see app/integrations/notifications/webpush.py
kafka-python==3.0.9  # Event bus - see app/integrations/eventbus/kafka.py
pgvector==0.5.0  # SQLAlchemy Vector column type - semantic search over AI summaries
slowapi==0.1.10  # rate limiting on login endpoints - see app/core/rate_limit.py
redis==5.0.8  # slowapi's shared Redis storage backend - see app/core/rate_limit.py
phonenumbers==8.13.45  # E.164 -> country parsing for the fraud engine's geographic-dispersion signal - see app/risk/service.py

# Observability (core/telemetry.py, core/logging.py) - OpenTelemetry tracing
# + metrics, OTLP-over-HTTP exporter. Off by default (OTEL_ENABLED=false);
# no new paid backend required - a blank OTEL_EXPORTER_OTLP_ENDPOINT prints
# to the console instead.
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-sqlalchemy==0.65b0
opentelemetry-instrumentation-httpx==0.65b0
opentelemetry-exporter-otlp-proto-http==1.44.0

# dev/test
pytest==8.3.3
httpx==0.27.2
