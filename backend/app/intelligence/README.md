Not started yet. Built in Stage 10, after core calling/voicemail is solid.
Planned: AI voicemail summaries, then AI Receptionist, then semantic search.
Only becomes its own deployed service if it later needs independent scaling
(e.g. GPU inference) — until then it stays a module inside the backend app.
