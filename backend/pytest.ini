[pytest]
markers =
    live: exercises a real third-party provider (Groq, LiveKit) and requires
        real credentials configured via .env - excluded from CI by default
        (see .github/workflows/ci.yml), run locally with `pytest -m live` or
        the full suite with real keys set.
